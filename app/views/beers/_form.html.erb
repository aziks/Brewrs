<%= form_for [ @beer] do |f| %>
  <% if @beer.errors.any? %>
    <div class="notification is-danger" id="error_explanation">
      <h2><%= pluralize(@beer.errors.count, "error") %> prohibited this beer from being saved:</h2>
      <ul>
      <% @beer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <p class="control">
  <%= f.label :name %>
  <br>
  <%= f.text_field :name %> 
  
  </p>
  <p class="control">
  <%= f.label :description %>
  <br>
  <%= f.text_area :description, :rows => 5, :cols => 50 %>
  
  </p>
  <p class="control">
<!--   PREVIEW IMAGE -->  
  <div class="column is-half" id="target"></div> 
 
  <%= f.file_field :beer_image, id: "pictureInput" %>
  <br>
  <br>
  </p>

  <button class="button is-success" type="submit" >
    <i class="fa fa-check"></i>
    Add new Beer!!
  </button>

<% end %>
