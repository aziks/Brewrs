<div class="container">
<h1 class="title is-1"> RECIPES INDEX </h1>
<hr>
</div>

<div class="columns is-multiline is-mobile">
  
  <% @recipes.each do |recipe| %>
   
    <div class="column is-quarter">
      <div class="container">
      RECIPE IMAGE
      </div>
      <a href="<%= recipe_path(recipe) %>"> <%= recipe.title %> </a> 
    </div>
    
 <% end %>

</div>  