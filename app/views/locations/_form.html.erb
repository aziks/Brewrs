<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div id="show_address" class="box"> </div>

  <input type="hidden" id="direction" name="location[address]">
  <input type="hidden" id="lat" name="location[lat]">
  <input type="hidden" id="lng" name="location[long]">

  <button class="button is-success" type="submit" >
    <i class="fa fa-check"></i>
    Add new location for this beer!!
  </button>
<% end %>
